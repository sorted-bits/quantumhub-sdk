export enum DeviceType {
  button = 'button',
  climate = 'climate',
  device_automation = 'device_automation',
  device_tracker = 'device_tracker',
  number = 'number',
  scene = 'scene',
  select = 'select',
  sensor = 'sensor',
  switch = 'switch',
}
