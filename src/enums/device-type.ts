export enum DeviceType {
  button = 'button',
  climate = 'climate',
  device_tracker = 'device_tracker',
  fan = 'fan',
  light = 'light',
  number = 'number',
  scene = 'scene',
  select = 'select',
  sensor = 'sensor',
  switch = 'switch',
}
